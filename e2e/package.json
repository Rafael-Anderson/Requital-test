{
  "name": "e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright browser E2E specs spanning backend + admin + storefront — see README.md",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
